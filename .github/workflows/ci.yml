name: CI

on:
  push:
    branches: [ main ]
    tags-ignore: [ '**' ]
  pull_request:

permissions:
  contents: read

jobs:
  ubuntu:
    uses: 9506hqwy/actions/.github/workflows/go-ci-ubuntu.yml@main
    with:
      enable-lint: '0'

  windows:
    uses: 9506hqwy/actions/.github/workflows/go-ci-windows.yml@main
